services:
  - type: web
    name: btb-fit
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PASSWORD_HASH
        generateValue: true
      - key: NODE_ENV
        value: production
